/**
 * Internal interfaces barrel.
 *
 * Cross-package shapes (`IQueueConnection`, `IQueueModuleOptions`,
 * `IJobOptions`, `IQueuedJob`, ...) live in `@stackra/contracts`.
 *
 * @module @stackra/nestjs-queue/interfaces
 */

export type { INestjsQueueModuleOptions } from './nestjs-queue-module-options.interface';
